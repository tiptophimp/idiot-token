# Cursor — rules entry point (idiot-token)

> HARD RULE — never echo credentials.

REQUIRED READING:

1. `E:\Dev\_shared\configs\AGENT_RULES.md`
2. `AGENTS.md` (this repo)
3. `E:\Dev\_shared\configs\FLEET_AGENT_ROSTER.md`

Branch prefix: `agent/cursor/<task-id>-<short-slug>`.

**Merge policy (2026-07-12):** squash-merge your own PR after required CI checks (`gh pr merge --squash` / `--auto`). Never direct-push main.
